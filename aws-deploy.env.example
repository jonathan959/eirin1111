# Copy to aws-deploy.env (gitignored) for automated EC2 start + security group fix.
# IAM user needs: ec2:Describe*, ec2:StartInstances, ec2:AuthorizeSecurityGroupIngress
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-2
