# Limit journal logs to prevent disk fill on EC2
# Deploy copies to /etc/systemd/journald.conf.d/ and runs systemctl restart systemd-journald
[Journal]
SystemMaxUse=50M
SystemMaxFileSize=10M
MaxRetentionSec=3day
